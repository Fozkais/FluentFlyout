<controls:MicaWindow x:Class="FluentFlyoutWPF.Windows.QueueWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
      mc:Ignorable="d" 
      Height="380" Width="340"
      ResizeMode="NoResize"
      WindowStyle="None" ShowInTaskbar="False"
      SystemBackdropType="Mica" ChangeTitleColorWhenInactive="False"
      TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal"
      FontFamily="{Binding FontFamily}"
      FlowDirection="{Binding FlowDirection}"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:SymbolIcon Symbol="List20" Filled="True" FontSize="18" Margin="0,0,8,0"/>
                    <TextBlock Text="File d'attente" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Name="ContextLabel" Text="" FontSize="11" Opacity="0.55" Margin="0,2,0,0"/>
            </StackPanel>
            <ui:Button Name="CloseButton" Appearance="Transparent" Icon="{ui:SymbolIcon Dismiss20}" HorizontalAlignment="Right" VerticalAlignment="Top" Click="CloseButton_Click"/>
        </Grid>

        <!-- Loading / Queue List -->
        <Grid Grid.Row="1">
            <ProgressBar Name="LoadingBar" IsIndeterminate="True" Height="4" VerticalAlignment="Top" Visibility="Collapsed"/>
            <TextBlock Name="EmptyMessage" Text="Aucun morceau dans la file d'attente." HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" Visibility="Collapsed"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl Name="QueueListView">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="6" Padding="6,4" Margin="0,2">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource MicaWPF.Brushes.ControlFillColorSecondary}"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource MicaWPF.Brushes.ControlFillColorTertiary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="36"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Cover -->
                                    <Border Grid.Column="0" CornerRadius="4" ClipToBounds="True" Width="36" Height="36">
                                        <Image Source="{Binding CoverUrl}" Stretch="UniformToFill"/>
                                    </Border>

                                    <!-- Title / Artist -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,8,0">
                                        <TextBlock Text="{Binding Title}" FontWeight="Medium" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Artist}" FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <!-- Play / Playing Status Button -->
                                    <ui:Button Grid.Column="2"
                                               Appearance="Transparent"
                                               Icon="{ui:SymbolIcon Play20, Filled=True}"
                                               ToolTip="Jouer ce titre"
                                               Width="32" Height="32"
                                               Padding="0"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               Click="PlayTrackButton_Click">
                                        <ui:Button.Style>
                                            <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                        <Setter Property="Icon" Value="{ui:SymbolIcon Speaker220, Filled=True}"/>
                                                        <Setter Property="ToolTip" Value="En cours de lecture"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:Button.Style>
                                    </ui:Button>

                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- Auth Footer -->
        <Border Grid.Row="2" Margin="0,10,0,0" Padding="8,4" Background="{DynamicResource MicaWPF.Brushes.ControlFillColorDefault}" CornerRadius="6">
            <Grid>
                <TextBlock Name="AuthStatusText" Text="Mode Connecté" FontSize="11" VerticalAlignment="Center" Opacity="0.7"/>
                <ui:Button Name="AuthButton" Content="Se connecter à Deezer" Appearance="Primary" HorizontalAlignment="Right" Click="AuthButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</controls:MicaWindow>
