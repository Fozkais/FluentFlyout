<controls:MicaWindow x:Class="FluentFlyoutWPF.Windows.QueueWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
      mc:Ignorable="d" 
      Height="380" Width="340"
      ResizeMode="NoResize"
      WindowStyle="None" ShowInTaskbar="False"
      SystemBackdropType="Mica" ChangeTitleColorWhenInactive="False"
      TextOptions.TextRenderingMode="ClearType" TextOptions.TextFormattingMode="Ideal"
      FontFamily="{Binding FontFamily}"
      FlowDirection="{Binding FlowDirection}"
      PreviewMouseLeftButtonUp="QueueWindow_PreviewMouseLeftButtonUp"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:SymbolIcon Symbol="List20" Filled="True" FontSize="18" Margin="0,0,8,0"/>
                    <TextBlock Text="File d'attente" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Name="ContextLabel" Text="" FontSize="11" Opacity="0.55" Margin="0,2,0,0"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <ui:Button Name="PlaylistButton" Appearance="Transparent" Icon="{ui:SymbolIcon Library20}" Margin="0,0,4,0" ToolTip="Playlists du compte" Click="PlaylistButton_Click"/>
                <ui:Button Name="RefreshButton" Appearance="Transparent" Icon="{ui:SymbolIcon ArrowSync20}" Margin="0,0,4,0" ToolTip="Actualiser la file d'attente" Click="RefreshButton_Click"/>
                <ui:Button Name="SearchToggleButton" Appearance="Transparent" Icon="{ui:SymbolIcon Search20}" Margin="0,0,4,0" ToolTip="Rechercher un titre" Click="SearchToggleButton_Click"/>
                <ui:Button Name="CloseButton" Appearance="Transparent" Icon="{ui:SymbolIcon Dismiss20}" ToolTip="Fermer" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Search Bar (Collapsible) -->
        <ui:TextBox Name="SearchTextBox" Grid.Row="1" PlaceholderText="Rechercher un titre ou un artiste..." ClearButtonEnabled="True" Margin="0,0,0,8" Visibility="Collapsed" TextChanged="SearchTextBox_TextChanged"/>

        <!-- Playlist Selector (Collapsible) -->
        <Border Name="PlaylistSelectorContainer" Grid.Row="1" Background="{DynamicResource MicaWPF.Brushes.ControlFillColorDefault}" CornerRadius="8" Padding="6" Margin="0,0,0,8" Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="180"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Vos Playlists Deezer" FontSize="12" FontWeight="SemiBold" Margin="4,2,0,6" Opacity="0.8"/>
                <ProgressBar Name="PlaylistLoadingBar" Grid.Row="1" IsIndeterminate="True" Height="3" VerticalAlignment="Top" Visibility="Collapsed"/>
                <ListView Name="PlaylistListView" Grid.Row="1" Height="180"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          Background="Transparent" BorderThickness="0"
                          SelectionChanged="PlaylistListView_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="4" CornerRadius="4" Cursor="Hand" Background="Transparent">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="36"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border CornerRadius="4" ClipToBounds="True" Width="32" Height="32">
                                        <Image Source="{Binding CoverUrl}" Stretch="UniformToFill"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                        <TextBlock Text="{Binding Title}" FontWeight="Medium" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding TrackCount, StringFormat='{}{0} titres'}" FontSize="10" Opacity="0.6"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Loading / Queue List -->
        <Grid Grid.Row="2">
            <ProgressBar Name="LoadingBar" IsIndeterminate="True" Height="4" VerticalAlignment="Top" Visibility="Collapsed"/>
            <TextBlock Name="EmptyMessage" Text="Aucun morceau dans la file d'attente." HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" Visibility="Collapsed"/>

            <ListView Name="QueueListView"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent"
                      BorderThickness="0"
                      Padding="0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="ItemBorder" CornerRadius="6" Padding="6,4" Margin="0,1"
                                Cursor="Hand"
                                PreviewMouseLeftButtonDown="QueueItem_PreviewMouseLeftButtonDown"
                                PreviewMouseMove="QueueItem_PreviewMouseMove"
                                PreviewMouseLeftButtonUp="QueueItem_PreviewMouseLeftButtonUp">
                            <Border.RenderTransform>
                                <TranslateTransform x:Name="ItemTranslateTransform" X="0" Y="0"/>
                            </Border.RenderTransform>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource MicaWPF.Brushes.ControlFillColorSecondary}"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource MicaWPF.Brushes.ControlFillColorTertiary}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Cover with Play Overlay on Hover -->
                                <Grid Grid.Column="0" Width="36" Height="36" Cursor="Hand" MouseLeftButtonDown="PlayCover_Click">
                                    <Border CornerRadius="4" ClipToBounds="True" Width="36" Height="36">
                                        <Image Source="{Binding CoverUrl}" Stretch="UniformToFill"/>
                                    </Border>
                                    <Border x:Name="PlayOverlay" CornerRadius="4" Background="#A0000000" Opacity="0" ToolTip="Jouer ce titre">
                                        <ui:SymbolIcon Symbol="Play20" Filled="True" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>

                                <!-- Title / Artist -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,8,0">
                                    <StackPanel Orientation="Horizontal">
                                        <ui:SymbolIcon Symbol="Speaker220" Filled="True" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center" Foreground="{DynamicResource MicaWPF.Brushes.SystemAccentColorPrimary}">
                                            <ui:SymbolIcon.Style>
                                                <Style TargetType="ui:SymbolIcon">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ui:SymbolIcon.Style>
                                        </ui:SymbolIcon>
                                        <TextBlock Text="{Binding Title}" FontWeight="Medium" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Artist}" FontSize="11" Opacity="0.6" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>

                                <!-- Delete Button ('X') -->
                                <ui:Button Grid.Column="2"
                                           Appearance="Transparent"
                                           Icon="{ui:SymbolIcon Dismiss16}"
                                           ToolTip="Supprimer de la file d'attente"
                                           Width="28" Height="28"
                                           Padding="0"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           Click="RemoveTrackButton_Click">
                                </ui:Button>

                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsMouseOver}" Value="True">
                                <Setter TargetName="PlayOverlay" Property="Opacity" Value="1"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</controls:MicaWindow>
